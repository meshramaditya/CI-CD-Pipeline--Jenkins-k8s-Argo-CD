apiVersion: v1
kind: Service
metadata:
  name: blue-cube-cafe-frontend
  labels:
    app: blue-cube-cafe-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: blue-cube-cafe-frontend
